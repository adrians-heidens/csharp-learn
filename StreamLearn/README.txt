Demonstrates stream usage.

A stream is an abstraction of a sequence of bytes, such as a file, an input/
output device, an inter-process communication pipe, or a TCP/IP socket.

* You can read from streams.
* You can write to streams.
* Streams can support seeking.

* Stream (abstract)
* FileStream
* MemoryStream
* BufferedStream

* Implements IDisposable.